# Token efficiency and LLM-call counts: paired t-test (full method vs BM25 baseline)

Recomputation of the paper's efficiency comparison using a **paired t-test** (`scipy.stats.ttest_rel`) as the primary significance test, replacing the paired Wilcoxon signed-rank test previously reported in `analysis/token_efficiency.py` / `analysis/token_efficiency.md`. Comparison: full method (`agent_research_bql_dense_snip`, comparison_result.md's **bql+dense+snip fetch**) vs the BM25 search-and-visit baseline (`agent_research_bm25`, comparison_result.md's **SERP bm25 [BASELINE]**), paired on shared `instance_id`s within each dataset. Data loading, file paths, and the count-once token fallback are copied verbatim from `analysis/token_efficiency.py` so the instance sets and token definition are identical to what the paper already reports elsewhere -- only the test changes.

Statistical test: scipy.stats.ttest_rel (v1.15.3) used for the paired t-test. The paired Wilcoxon signed-rank test (`scipy.stats.wilcoxon`) is also computed and reported for audit/comparison only, so any change in conclusion is visible.

Generated by `analysis/paired_ttests.py` (`PYTHONPATH=. envs/bin/python analysis/paired_ttests.py`).

## browsecomp_plus_structured

n_shared = 830 (method cell n=830, baseline cell n=830)

### Token cost (total_tokens_once, count-once basis)

| n | mean method | mean baseline | mean diff (base-method) | 95% CI | t | df | p (t-test) | Cohen's d | % reduction | Wilcoxon p (audit) |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 830 | 44.91k | 63.80k | 18.90k | [14.66k, 23.13k] | 8.755 | 829 | 1.133e-17 | 0.304 | 29.6% | 1.454e-33 |

Normality of paired differences: skew = -0.864, excess kurtosis = 16.910, Shapiro-Wilk p = 1.932e-29 (n=830 -- full sample, seed=0).

### LLM calls per episode

| n | mean method | mean baseline | mean diff (base-method) | 95% CI | t | df | p (t-test) | Cohen's d | % reduction | Wilcoxon p (audit) |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 830 | 60.69 | 62.45 | +1.77 | [-1.39, +4.93] | 1.097 | 829 | 0.273 | 0.038 | 2.8% | 0.475 |

Normality of paired differences: skew = 0.073, excess kurtosis = -0.187, Shapiro-Wilk p = 1.008e-18 (n=830 -- full sample, seed=0).

Note: mean diff / t / % reduction here are all defined baseline-minus-method for calls too (t computed via `ttest_rel(baseline, method)`), so a **negative** value means the method makes *more* calls than the baseline (reported honestly, not hidden, not just on the wiki datasets where this is in fact the case).

## hotpotqa_structured

n_shared = 7343 (method cell n=7343, baseline cell n=7343)

### Token cost (total_tokens_once, count-once basis)

| n | mean method | mean baseline | mean diff (base-method) | 95% CI | t | df | p (t-test) | Cohen's d | % reduction | Wilcoxon p (audit) |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 7343 | 13.87k | 19.94k | 6.07k | [5.62k, 6.52k] | 26.345 | 7342 | 3.132e-146 | 0.307 | 30.4% | 2.093e-101 |

Normality of paired differences: skew = 2.513, excess kurtosis = 9.661, Shapiro-Wilk p = 2.662e-65 (n=5000, random subsample of 5000, seed=0).

### LLM calls per episode

| n | mean method | mean baseline | mean diff (base-method) | 95% CI | t | df | p (t-test) | Cohen's d | % reduction | Wilcoxon p (audit) |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 7343 | 20.93 | 15.90 | -5.03 | [-5.39, -4.66] | -26.687 | 7342 | 7.723e-150 | -0.311 | -31.6% | 1.069e-226 |

Normality of paired differences: skew = -0.032, excess kurtosis = 1.868, Shapiro-Wilk p = 1.767e-47 (n=5000, random subsample of 5000, seed=0).

Note: mean diff / t / % reduction here are all defined baseline-minus-method for calls too (t computed via `ttest_rel(baseline, method)`), so a **negative** value means the method makes *more* calls than the baseline (reported honestly, not hidden, not just on the wiki datasets where this is in fact the case).

## musique_structured

n_shared = 2409 (method cell n=2409, baseline cell n=2409)

### Token cost (total_tokens_once, count-once basis)

| n | mean method | mean baseline | mean diff (base-method) | 95% CI | t | df | p (t-test) | Cohen's d | % reduction | Wilcoxon p (audit) |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 2409 | 21.27k | 43.33k | 22.06k | [20.86k, 23.26k] | 36.075 | 2408 | 3.270e-228 | 0.735 | 50.9% | 1.846e-212 |

Normality of paired differences: skew = 1.038, excess kurtosis = 0.303, Shapiro-Wilk p = 1.553e-37 (n=2409 -- full sample, seed=0).

### LLM calls per episode

| n | mean method | mean baseline | mean diff (base-method) | 95% CI | t | df | p (t-test) | Cohen's d | % reduction | Wilcoxon p (audit) |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 2409 | 32.59 | 29.65 | -2.94 | [-3.70, -2.19] | -7.620 | 2408 | 3.616e-14 | -0.155 | -9.9% | 9.765e-27 |

Normality of paired differences: skew = 0.741, excess kurtosis = 2.773, Shapiro-Wilk p = 1.547e-34 (n=2409 -- full sample, seed=0).

Note: mean diff / t / % reduction here are all defined baseline-minus-method for calls too (t computed via `ttest_rel(baseline, method)`), so a **negative** value means the method makes *more* calls than the baseline (reported honestly, not hidden, not just on the wiki datasets where this is in fact the case).

## What changed vs Wilcoxon

No conclusion flips: for every dataset and both metrics (tokens, LLM calls), the paired t-test and the paired Wilcoxon signed-rank test agree on significance at α=0.05. p-values differ numerically (t-test p-values are generally smaller/larger depending on the shape of the difference distribution, see the normality diagnostics above per dataset) but no comparison changes from significant to non-significant or vice versa.

Per-dataset normality caveat: see the skew/kurtosis/Shapiro-Wilk lines above. Where the paired differences are strongly right-skewed / leptokurtic and Shapiro-Wilk p is very small, the differences are formally non-normal; the t-test is still a defensible choice at these sample sizes on Central Limit Theorem grounds (the *sampling distribution of the mean* difference, not the difference distribution itself, is what needs to be approximately normal for the t-test's validity), but this should be stated plainly rather than implied to be a non-issue.
