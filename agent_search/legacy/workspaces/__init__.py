"""Pre-0.3 tool surface: each workspace module in this package hosts a family of tools, and a
run enables a subset (its toolset). Kept so the parity tests can compare against it.
`agent_search/tools/` (one folder per atomic tool) replaces this."""
