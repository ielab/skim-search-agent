.idea/easycode.ignore
/.idea

# Local environment / secrets / per-machine agent notes
envs/
.venv/
.env
CLAUDE.md

# Data, indexes, run outputs (large; pull or rebuild per corpus_build/README.md)
data/
indexes/
runs/
logs/
slurm_logs/

# Python
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
/demo/app/node_modules
/demo/app/dist
.DS_Store
build/
dist/

# Local working material (slides, ad-hoc outputs, test reports) — never part of the release
*.pptx
report.xml
*.junit.xml
comparison_result.md
analysis/.compare_cache/
analysis/paper_tables_cache.json
envs-train/
train_data/
/models/

# The changelog and the publishing notes are kept locally, not published
CHANGELOG.md
docs/PUBLISHING.md
slurm-*.out
configs/checks/
